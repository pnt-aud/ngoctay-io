{
  "name": "ngoctay-io",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "postbuild": "npx @cloudflare/next-on-pages",
    "start": "next start",
    "cf:build": "npm run build",
    "cf:deploy": "npm run build && wrangler pages deploy .vercel/output/static --project-name=ngoctay-io --functions .vercel/output/functions",
    "cf:preview": "npm run build && wrangler pages deploy .vercel/output/static --project-name=ngoctay-io --branch=preview --functions .vercel/output/functions"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "next": "14.2.5",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.17.7",
    "@cloudflare/next-on-pages": "^1",
    "wrangler": "^3",
    "typescript": "^5.6.3",
    "eslint": "^8.57.0",
    "eslint-config-next": "14.2.5"
  }
}
